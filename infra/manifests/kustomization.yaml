# GitOps source for ArgoCD (clearledger-infra). CI updates image tags here only.
# Deployments use placeholders (clearledger/*:gitops); Kustomize maps to registry + SHA.
#
# Stage 1.3: replace YOUR_DOCKERHUB_USERNAME below with your Docker Hub user before first push.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: clearledger

resources:
  - namespace.yaml
  - ingress.yaml
  - auth-service/deployment.yaml
  - auth-service/service.yaml
  - auth-service/secret.yaml
  - ledger-service/deployment.yaml
  - ledger-service/service.yaml
  - ledger-service/secret.yaml
  - notification-service/deployment.yaml
  - notification-service/service.yaml
  - frontend/deployment.yaml
  - postgres/postgres.yaml
  - postgres/postgres-secret.yaml
  - redis/redis.yaml
  - rbac/rbac.yaml

images:
  - name: clearledger/auth-service
    newName: docker.io/imorti/clearledger-auth-service
    newTag: v0.1.0
  - name: clearledger/ledger-service
    newName: docker.io/imorti/clearledger-ledger-service
    newTag: v0.1.0
  - name: clearledger/notification-service
    newName: docker.io/imorti/clearledger-notification-service
    newTag: v0.1.0
  - name: clearledger/frontend
    newName: docker.io/imorti/clearledger-frontend
    newTag: v0.1.0
